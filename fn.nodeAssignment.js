var roleHarvester = {

    /** @param {Creep} creep **/
    run: function(creeprole, dedicated) {
        //generate node assignment and return it

        //if overloaded, percentage will determine new node assignment
        
    
        return()
    }
}
module.exports = nodeAssign;